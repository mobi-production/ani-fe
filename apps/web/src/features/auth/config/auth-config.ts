export const END_POINT = {
  AUTH: {
    LOGIN: '/auth/login',
    SIGNUP: '/auth/signup'
  }
}

export const QUERY_KEYS = {
  AUTH: {
    LOGIN: ['authLogin'] as string[],
    SIGNUP: ['authSignup'] as string[]
  }
}

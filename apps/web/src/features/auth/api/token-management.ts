type TokenType = {
  accessToken: string
  refreshToken: string
}

const setToken = async (token: TokenType) => {
  return
}

const updateToken = async () => {
  return
}

const removeToken = async () => {
  return
}

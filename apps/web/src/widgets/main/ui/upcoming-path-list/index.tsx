import { PathData } from '@/__mock__/data/main'

type UpcomingPathListProps = {
  pathData: PathData
}

function UpcomingPathList({ pathData }: UpcomingPathListProps) {
  return <div>UpcomingPathList</div>
}

export default UpcomingPathList
